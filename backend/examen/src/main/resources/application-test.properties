# Test profile configuration - no database needed for basic context load test
spring.application.name=examen-test
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.JdbcAutoConfiguration
